package com.karorimesh.projectmangement.service;

import com.karorimesh.projectmangement.dto.PropertyDTO;

public interface PropertyService {
    public PropertyDTO saveProperty (PropertyDTO propertyDTO);
}
